adapters:
  codex:
    enabled: false
    model: "gpt-4o"
    timeout_seconds: 300
    sandbox: none
  gemini:
    enabled: true
    model: "pro"
    timeout_seconds: 600
    sandbox: none

routing:
  default_implementer: "gemini"
  escalation_order: ["gemini", "claude"]

budget:
  per_task_usd: 5.00
  per_session_usd: 40.00
